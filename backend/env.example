# EduManage Backend Environment Variables
# Copy this file and name it as .env in the backend folder

PORT=5000
MONGODB_URI=mongodb://localhost:27017/edumanage
JWT_SECRET=edumanage_secret_key_2025_change_this_in_production
NODE_ENV=development

# For MongoDB Atlas (Cloud Database):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/edumanage?retryWrites=true&w=majority

# Note: If .env file doesn't work, the config.js file will be used as fallback

